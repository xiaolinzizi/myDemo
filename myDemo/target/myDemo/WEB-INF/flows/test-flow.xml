<?xml version="1.0" encoding="UTF-8"?>
<flow xmlns="http://www.springframework.org/schema/webflow"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.springframework.org/schema/webflow
 http://www.springframework.org/schema/webflow/spring-webflow-2.4.xsd">
    <!--
    <view-state id="welcome" view="test"></view-state>
    <action-state id="saveOrder">
        <evaluate expression="pizzFlowControler.saveoder()"/>
        <transition to="thankyou"></transition>
    </action-state> -->
    <!--
    <var name="order" class="com.cn.myself.entity.Order"></var>
    <subflow-state id="identifyCustomer" subflow="pizza/customer">
        <output name="customer" value="order.customer"/>
        <transition on="customerReady" to="buildOrder"/>
    </subflow-state>

    <subflow-state id="buildOrder" subflow="pizza/order">
        <input name="order" value="order"></input>
    </subflow-state>
    -->
</flow>